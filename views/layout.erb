<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>タイトル</title>
</head>
<body>
<% unless session[:user] %>
  <a href="/signin">ログイン</a> <a href="/signup">新規登録</a><br>
  <h2>未ログイン</h2>
  <% else %>

  <a href="/signout">ログアウト</a>

<% if current_user.remained_days == 0 %>

  <h2><%= User.find(session[:user]).mail%>さん誕生日おめでとう</h2>
  <% else %>
  <h2><%= User.find(session[:user]).mail%>さんとしてログイン</h2>

  <% end %>


　
  <% end %>

  <%= yield %>
</body>
</html>